# Local Backstage Plugins
Place your plugin packages here (each with its own package.json). Then add the plugin to:
- `packages/app/src/App.tsx` for frontend routes/components
- `packages/backend/src/index.ts` for backend routers if needed
